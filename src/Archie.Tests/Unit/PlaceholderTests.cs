namespace Archie.Tests.Unit;

/// <summary>
/// Placeholder — replace with real tests as Core logic is implemented.
/// Follow the naming convention: {ClassUnderTest}Tests / {Method}_When{Condition}_Should{Result}
/// </summary>
public sealed class PlaceholderTests
{
    [Fact]
    public void Solution_WhenBuilt_ShouldCompile()
    {
        // Intentionally empty — this test exists only to keep the test runner happy
        // until real unit tests are written.
    }
}
